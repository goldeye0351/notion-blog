name: auto-redeploy
on:
  schedule:
    - cron: '10 * * * *'
jobs:
  build:
    name: daysredeploy
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel Hook
        run: curl -X POST "https://api.vercel.com/v1/integrations/deploy/${VERCEL_PROJECT_ID}/${VERCEL_HOOK_ID}?buildCache=false"
